import { HostDirective } from './host.directive';

describe('HostDirective', () => {
  it('should create an instance', () => {
    // TODO test
    const directive = new HostDirective(null);
    expect(directive).toBeTruthy();
  });
});
