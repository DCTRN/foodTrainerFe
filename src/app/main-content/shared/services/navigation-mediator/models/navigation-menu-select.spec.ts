import { NavigationMenuSelect } from './navigation-menu-select';

describe('NavigationMenuSelect', () => {
  it('should create an instance', () => {
    expect(new NavigationMenuSelect()).toBeTruthy();
  });
});
